# outputs grandes ficam em /workspace/tools e /workspace/run_pipeline outputs
# Mantem so metadados no repo, não binários

*.pyc
__pycache__/
.env
*.key
*.pem

# Mas NÃO ignora os outputs reais — eles provam que funciona!
# Na verdade IGNORA os tree/* e .obj raw (muito grandes)
